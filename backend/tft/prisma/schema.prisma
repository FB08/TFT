generator client {
  provider = "prisma-client"
  output   = "../generated"
}

datasource db {
  provider = "mysql"
}

model curriculum {
  id                String              @id @default(uuid())
  userId            String
  title             String
  description       String?
  course            String?
  book              String?
  difficulty        String?
  memo              String?
  createdAt         DateTime            @default(now())
  user              user                @relation(fields: [userId], references: [userId])
  curriculumlessons curriculumlessons[]

}

model curriculumlessons {
  id           String     @id @default(uuid())
  curriculumId String
  description  String?
  progress     String?
  homework     String?
  order        Int
  createdAt    DateTime   @default(now())
  curriculum   curriculum @relation(fields: [curriculumId], references: [id])
}

model homework {
  id             String          @id @default(uuid())
  lessonId       String
  content        String
  status         homework_status @default(ASSIGNED)
  assignedAt     DateTime        @default(now())
  carriedFromId  String?
  createdAt      DateTime        @default(now())
  homework       homework?       @relation("homeworkTohomework", fields: [carriedFromId], references: [id])
  other_homework homework[]      @relation("homeworkTohomework")
  lessonrecord   lessonrecord    @relation(fields: [lessonId], references: [id])
}

model lessonexception {
  id              String        @id @default(uuid())
  patternId       String
  originalStartAt DateTime
  isCanceled      Boolean       @default(false)
  newStartAt      DateTime?
  newEndAt        DateTime?
  createdAt       DateTime      @default(now())
  lessonpattern   lessonpattern @relation(fields: [patternId], references: [id])
  @@unique([patternId, originalStartAt], name: "lessonPatternId_originalStartAt")

}

model lessonpattern {
  id              String            @id @default(uuid())
  studentId       String
  startTime       String
  endTime         String
  startRecur       DateTime
  endRecur         DateTime?
  rrule           String
  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  lessonexception lessonexception[]
  student         student           @relation(fields: [studentId], references: [id])
}

model lessonrecord {
  id            String     @id @default(uuid())
  studentId     String
  lessonStartAt DateTime
  progress      String?
  feedback      String?
  createdAt     DateTime   @default(now())
  homework      homework[]
  student       student    @relation(fields: [studentId], references: [id])
  todo          todo[]
}

model singlelesson {
  id        String   @id @default(uuid())
  studentId String
  startAt   DateTime
  endAt     DateTime
  createdAt DateTime @default(now())
  student   student  @relation(fields: [studentId], references: [id])
}

model student {
  id            String          @id @default(uuid())
  userId        String
  name          String?
  age           Int?
  sex           student_sex?
  address       String?
  phone         String?
  parentPhone   String?
  book          String?
  memo          String?
  payRate       Int?
  startedAt     DateTime        @default(now())
  createdAt     DateTime        @default(now())
  updatedAt     DateTime        @updatedAt
  lessonpattern lessonpattern[]
  lessonrecord  lessonrecord[]
  singlelesson  singlelesson[]
  user          user            @relation(fields: [userId], references: [userId])
}

model todo {
  id           String        @id @default(uuid())
  userId       String
  lessonId     String?
  content      String
  until        DateTime?
  createdAt    DateTime      @default(now())
  lessonrecord lessonrecord? @relation(fields: [lessonId], references: [id])
  user         user          @relation(fields: [userId], references: [userId])
}

model user {
  id               String       @id @default(uuid())
  userId           String       @unique
  passwordHash     String
  deletedAt        DateTime?
  createdAt        DateTime     @default(now())
  updatedAt        DateTime     @updatedAt
  refreshTokenHash String       @db.VarChar(255)
  name             String       @db.VarChar(20)
  curriculum       curriculum[]
  student          student[]
  todo             todo[]
}

enum homework_status {
  ASSIGNED
  COMPLETE
  CARRIED_OVER
  CANCELED
}

enum student_sex {
  Male
  Female
}
